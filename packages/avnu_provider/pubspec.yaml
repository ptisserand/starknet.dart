name: avnu_provider
description: Avnu provider for Dart and Flutter applications.
version: 0.1.0
repository: https://github.com/focustree/starknet.dart

environment:
  sdk: ^3.6.0

resolution: workspace

# Add regular dependencies here.
dependencies:
  starknet: ^0.2.0
  http: ^1.3.0
  starknet_provider: ^0.2.0
  # versions contrainted by workspace
  freezed_annotation:
  json_annotation:

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:
  test: ^1.25.15